import { useState } from "react";

//useState는 항상 2개의 value를 return한다.
function UseState(){

    const [item, setItem] = useState(); //초기 값 세팅 할 수 있는 옵션 제공.

    return(
        <div></div>
    );
}

export default UseState;